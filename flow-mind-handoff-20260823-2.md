# 引き継ぎメモ: flow-mind 開発状況（2026-08-23 追記版）

## このメモの位置づけ

前回メモ（2026-08-23 最終更新版）に対する追記。今回のセッションでは実装は行わず、
「quick-ref連携をもっと使いやすくする」というバックログ項目について、着手前のヒアリングと
設計方針の合意までを行った。次のセッションはこのメモの「次にやること」からそのまま着手できる。

---

## 今回決まったこと（ヒアリング結果）

quick-ref連携について「何が使いにくいか」を確認した結果：

- **一番の不便**：取り込まれたノードの扱いが分かりにくい（送信→ポーリング受信はできているが、
  取り込まれた後どう扱われているかが不透明）
- **sticky note連携の想定**：Windows付箋アプリ（Sticky Notes）とのことで確定。
  flow-mind内の簡易メモ機能の話ではない
- **自動タグ抽出の用途**：「すべて」との回答。ノード分類の自動化／quick-refとの共通タグでの
  連携強化／検索・整理のしやすさ全般、の3つを1つの仕組みでまかなう方針
- **着手順序**：以下の順で1つずつ進める（並行ではなく順番に）

---

## 次にやること（優先順）

### 1. 取り込みノードのUI改善（最優先・次にこれから着手）

現状: quick-ref由来のノードが通常ノードと見た目上区別できず、取り込み後の状態も追えない。

対応方針：
- quick-ref由来ノードに出所バッジ（例：📥）と薄い色分けを追加し、視覚的に区別できるようにする
- トリアージ状態 `未処理 → 取り込み済み → アーカイブ` を導入。未処理ノードはキャンバス上の
  専用インボックス的レーン（画面端など）に集約表示する
- 「取り込み完了」アクションでステータスを変更できるようにする
- ここでの状態管理は、将来的な双方向化（flow-mind側の処理状況をquick-ref側へ書き戻す）の
  布石にもなる設計にしておくこと（バックログの`SyncBridge.autoLoad`引数不一致修正とは別件だが、
  同じ`shared/`配下の同期まわりを触るので、着手時に既存コードとの整合を確認すること）

### 2. Windows付箋（Sticky Notes）連携

公式APIが存在しないため、以下いずれかの方式で実装する（推奨は前者）：

- **ローカルブリッジ方式（推奨）**：Windows付箋は
  `%LOCALAPPDATA%\Packages\Microsoft.MicrosoftStickyNotes_.../LocalState/plum.sqlite`
  にデータを持っている（非公式だが読み取り可能）。開発機がWindows + Live Serverの環境である
  ことを利用し、Node.jsの小さなブリッジスクリプトでこのDBを定期監視し、差分を既存の
  Cloudflare Worker（`sync-worker`、KVネームスペース`FLOW_MIND_KV`）経由でflow-mindへ
  push する構成。既存のCloudSyncインフラをそのまま流用できる
  - 注意点：`plum.sqlite`の構造は非公式情報のため、Windows Updateで変わるリスクがある。
    実装時はスキーマ変化を検知するとエラーメッセージが出るようガードを入れておくこと
- **簡易代替案**：自動化を見送り、付箋の内容を手動でquick-refのクイック入力欄にコピペする
  運用に留める。ブリッジ方式の労力対効果を見て、こちらに倒す判断もあり得る

### 3. 自動タグ抽出モジュール（共通化）

新規ファイル `shared/tag-extractor.js` を作成し、flow-mindのメモ欄・クイック記録欄と
quick-ref側の両方から呼び出せる共通ロジックにする。

- 抽出方式は以下いずれか（実装時に選定）：
  - 軽量な形態素解析（kuromoji.js等、ブラウザ内完結でサーバー不要）
  - シンプルな頻出語抽出＋ストップワード除去（実装が軽い分、精度は形態素解析に劣る）
- 抽出したタグ候補は既存のdatalist機能（実装済み）に統合する
- 確定タグは`sync-worker`のKV経由でflow-mind／quick-ref間の共有タグ辞書として同期し、
  分類の自動化・連携強化・検索性向上の3目的を1つの仕組みでまかなう

---

## 前回メモからの継続事項（未対応のまま）

- `SyncBridge.autoLoad`の引数不一致（優先度: 中、前回メモ参照）
- iPhoneでの実機実装・検証（quick-ref連携の目処が立った後に着手する方針は継続）

---

## 現在の全ファイル構成（flow-mind専用、変更なし）

```
c:\Users\norin\flow-mind\
├── index.html
├── manifest.flow-mind.json
├── flow-mind-sw.js
└── shared\
    ├── sync-bridge.js
    ├── cloud-sync.js
    └── drive-keep-sync.js
```

次回はここに `shared/tag-extractor.js` が追加される予定。
